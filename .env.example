POSTGRES_USER=fingo
POSTGRES_PASSWORD=fingo_secret
POSTGRES_DB=fingo

DB_CONN_STRING=postgres://fingo:fingo_secret@localhost:5432/fingo?sslmode=disable

APP_PORT=8008
APP_DEBUG=true
APP_ENV=local

# PASETO v4 symmetric key (64 hex chars = 32 bytes).
# Оставьте пустым для dev (генерируется случайно при каждом старте).
TOKEN_SYMMETRIC_KEY=
TOKEN_DURATION=24h


test_user
"email": "lalala@example.com"
"name":"TestUser"
"password":"0123456"
